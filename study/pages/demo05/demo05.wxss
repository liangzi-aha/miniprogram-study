
/* 
1 小程序中 不需要主动来引入样式文件
2 需要把页面中某些元素的单位由 px 改成 rpx
    1 设计稿 750px
    750px = 750rpx
    1px = 1rpx
    2 把屏幕宽度改为 375px
    375px = 750rpx
    1px = 2rpx
3 利用一个属性 calc属性 css 和 wxss都支持 的属性
*/

view{
    /* width: 200rpx; */
    height: 200rpx;
    font-size: 40rpx;
    background-color: aliceblue;
    width:calc(750rpx * 100 / 375);
}