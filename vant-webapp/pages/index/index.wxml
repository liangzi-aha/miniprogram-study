<!--index.wxml-->
<view class="container">
  <van-button type="primary" bindtap="changeDialog">vant-web按钮</van-button>
  <mp-dialog title="test" show="{{dialog}}" bindbuttontap="tapDialogButton" buttons="{{buttons}}">
    <view>weUi弹框</view>
</mp-dialog>
</view>
